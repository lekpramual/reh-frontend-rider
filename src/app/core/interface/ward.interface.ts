export interface WardList {
  ward_id:string;
  ward_name: string;
  ward_status_id: string;
  ward_status_name: string;
  ward_date:string;
}

export interface WardCreate {
  ward_id:string;
  ward_name: string;
}
